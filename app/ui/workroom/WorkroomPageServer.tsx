import WorkroomServer from './components/WorkroomServer';

export default function WorkroomPageServer() {
    return (
        <main className="flex wrap w-full h-full flex-wrap mt-3 p-3 gap-2 justify-center">
            <WorkroomServer />
        </main>
    )
};