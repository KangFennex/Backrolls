export default function Lead() {
    return (
        <main className="lead">
            <h2 className="transform -rotate-12 font-black text-4xl italic tracking-wider uppercase bg-gradient-to-r from-pink-500 via-red-500 to-yellow-500 bg-clip-text text-transparent drop-shadow-lg w-fit">
                She said what?
            </h2>
        </main>
    );
}
